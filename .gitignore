.venv/
__pycache__/
*.pyc
.env
.env.local

node_modules/
.next/

# scratch dirs
.tmp-*/

# Telegram polling cursor — local state, not history
.telegram-offset

# Machine-specific MCP registration (absolute paths). Copy from .mcp.json.example
.mcp.json

# Your content. Its own private repo — never committed to the public one.
vault/

# autopilot run log — local, not content
autopilot.log

# Timestamp of the last brief actually delivered. Local state.
.last-brief
